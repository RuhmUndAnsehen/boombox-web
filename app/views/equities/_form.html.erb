<%= form_with(model: equity) do |form| %>
  <% if equity.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(equity.errors.count, "error") %> prohibited this equity from being saved:</h2>

      <ul>
        <% equity.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :symbol, style: "display: block" %>
    <%= form.text_field :symbol %>
  </div>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
